diff --git a/index.html b/index.html
index 1ffd7a9..5a477d7 100644
--- a/index.html
+++ b/index.html
@@ -9577,6 +9577,136 @@ document.addEventListener("click",function(e){
 })();
 /* =============== M8-END-OF-ALL-BLOCKS =============== */
 
+/* ================= M10-BLOCK-1: writer-lease client (design v9.1 §3/§4) =================
+   Increment 1 of the authorized LOCAL client work (Architect round 12,
+   server-package gate closed). MACHINERY ONLY: nothing outside the M10
+   blocks calls m10Gate yet — live behavior is unchanged until the
+   gate-wiring increment. The fence is the authority; deviceId is a
+   copyable label (D10). A persisted grant is NEVER sufficient to write
+   after reload: boot requires an online acquire/renew (monotonic clock
+   lost — design §4); failing that the device is read-only, fail closed. */
+var M10={fence:0,holder:false,deadline:0,known:null,corrupt:false,ttlMs:24*3600*1000,timer:null,booted:false};
+
+function m10DeviceId(){
+  /* stable per-install label; verified write (a device that cannot persist
+     its own id must not hold the pen — fail closed via corrupt state) */
+  try{
+    var id=localStorage.getItem("wl_m10_deviceid");
+    if(!id){
+      id="dev-"+Date.now().toString(36)+"-"+Math.random().toString(36).slice(2,10);
+      localStorage.setItem("wl_m10_deviceid",id);
+      if(localStorage.getItem("wl_m10_deviceid")!==id)return null;
+    }
+    return id;
+  }catch(e){return null;}
+}
+function m10DeviceName(){
+  var ua=navigator.userAgent||"";
+  if(/iPad/.test(ua)||(/Macintosh/.test(ua)&&navigator.maxTouchPoints>1))return "iPad";
+  if(/iPhone/.test(ua))return "iPhone";
+  if(/Android/.test(ua))return "Android";
+  if(/Macintosh/.test(ua))return "Mac";
+  return "Browser";
+}
+function m10GrantKey(){return "wl_m10_grant__"+pbUid();}
+function m10ReadGrant(){
+  /* informational persisted copy; a CORRUPT one still blocks (fail closed +
+     banner rule, design §4) because it evidences storage damage */
+  try{
+    var raw=localStorage.getItem(m10GrantKey());
+    if(raw==null)return null;
+    var g=JSON.parse(raw);
+    if(!g||typeof g.fence!=="number"||!g.deviceId)throw new Error("bad grant");
+    return g;
+  }catch(e){M10.corrupt=true;return null;}
+}
+function m10WriteGrant(g){
+  try{
+    var k=m10GrantKey(),v=JSON.stringify(g);
+    localStorage.setItem(k,v);
+    return localStorage.getItem(k)===v;
+  }catch(e){return false;}
+}
+function m10LeaseCall(op,extra){
+  var b={op:op,deviceId:m10DeviceId(),deviceName:m10DeviceName()};
+  if(extra)for(var k in extra)b[k]=extra[k];
+  var h={"Content-Type":"application/json"};var t=pbTok();if(t)h.Authorization=t;
+  return fetch(pbBase()+"/api/cf/writer/lease",{method:"POST",headers:h,body:JSON.stringify(b)})
+    .then(function(r){return r.json().then(function(j){return {status:r.status,body:j};});});
+}
+function m10ApplyGrant(body){
+  M10.fence=body.fence;M10.holder=true;M10.corrupt=false;
+  M10.deadline=performance.now()+(body.ttlMs||M10.ttlMs);
+  M10.known=body;
+  /* informational persisted copy — verified write, but failure here does not
+     revoke the in-session grant (the authority is the server fence + the
+     monotonic in-memory deadline; the copy only aids diagnostics) */
+  m10WriteGrant({fence:body.fence,deviceId:m10DeviceId(),grantedServerNow:body.serverNow,ttlMs:body.ttlMs});
+}
+function m10Boot(){
+  /* boot lease lifecycle: online acquire (fresh grant, own no-op refresh,
+     or held-by-other → read-only). Offline/failed → NOT holder (fail
+     closed); nothing gated in this increment, so live UX is unchanged. */
+  if(!syncOn())return Promise.resolve();
+  M10.booted=true;
+  var dev=m10DeviceId();
+  if(!dev){M10.corrupt=true;M10.holder=false;return Promise.resolve();}
+  m10ReadGrant();                                   /* corrupt detection only */
+  if(M10.corrupt){M10.holder=false;return Promise.resolve();}
+  return m10LeaseCall("acquire",{}).then(function(r){
+    if(r.status===200&&r.body&&r.body.ok){m10ApplyGrant(r.body);m10StartTimer();}
+    else if(r.status===409&&r.body&&r.body.held){M10.holder=false;M10.fence=0;M10.known=r.body;}
+    else{M10.holder=false;}
+  }).catch(function(){M10.holder=false;});
+}
+function m10Renew(){
+  if(!M10.holder)return Promise.resolve(false);
+  return m10LeaseCall("renew",{fence:M10.fence}).then(function(r){
+    if(r.status===200&&r.body&&r.body.ok){
+      M10.deadline=performance.now()+(r.body.ttlMs||M10.ttlMs);
+      return true;
+    }
+    /* typed stale (fence moved / released / expired): holder revoked NOW */
+    M10.holder=false;M10.known=r.body||null;
+    return false;
+  }).catch(function(){
+    /* transport failure: the existing monotonic deadline stands — holder
+       remains only within it (no extension without a server ack) */
+    return M10.holder&&performance.now()<M10.deadline;
+  });
+}
+function m10StartTimer(){
+  if(M10.timer)clearInterval(M10.timer);
+  M10.timer=setInterval(function(){
+    if(document.visibilityState==="visible"&&M10.holder)m10Renew();
+  },5*60*1000);
+}
+document.addEventListener("visibilitychange",function(){
+  if(document.visibilityState==="visible"&&M10.booted&&M10.holder)m10Renew();
+});
+function m10TakeoverSheet(){
+  var name=(M10.known&&M10.known.deviceName)?('"'+M10.known.deviceName+'"'):"another session";
+  askConfirm("Another session is the active writer ("+name+"). Take over on this device?",function(){
+    m10LeaseCall("steal",{}).then(function(r){
+      if(r.status===200&&r.body&&r.body.ok){
+        m10ApplyGrant(r.body);m10StartTimer();
+        toast("This device is now the active writer");render();
+      }else toast("Could not take over");
+    }).catch(function(){toast("Offline \u2014 taking over needs a connection");});
+  },{label:"Take over",danger:false});
+}
+function m10Gate(tag){
+  /* the write gate (design §5): TRUE only for a local-only app (no sync
+     account — no lease concept) or a current holder inside the monotonic
+     deadline. Everything else fails closed and offers the takeover sheet. */
+  if(!syncOn())return true;
+  if(M10.corrupt){toast("Sync identity problem \u2014 this device is read-only");return false;}
+  if(M10.holder&&performance.now()<M10.deadline)return true;
+  m10TakeoverSheet();
+  return false;
+}
+/* =============== M10-BLOCK-1-END =============== */
+
 function cloudTest(){
   if(ownershipAmbiguous()){toast("Paused — this device holds data from another account");return;}
   setSync("syncing");
@@ -10088,7 +10218,7 @@ if(!logoutRecoveryPending())runRecoveryCapture();
 if(bootGated()){
   renderGate();
 }else{
-load();loadTraining();migrateProgressionTypes();loadWorkout();rptRetargetAll();ensureIdentity();try{refreshWeekPhotos();}catch(e){}if(resyncAllActivityTags())save();var _hasSetup=/[#&]setup=/.test(location.hash||"");if(_hasSetup){state.settings.onboarded=true;}else{if(state.settings.onboarded==null)state.settings.onboarded=(state.weights.length>0||num(state.settings.goalWeight)!=null||num(state.settings.startingWeight)!=null||!!(state.settings.name&&(""+state.settings.name).trim()));if(!state.settings.onboarded){state.onboarding=true;state.obStep=0;}}applyTheme(state.settings.theme||"dark");makeIcon();if(state.workout)state.view="train";render();if(syncOn()){pbRefresh(function(){autoSync();photoSync();});}if(state.workout)startWoTick();if(_hasSetup)checkSetupLink();
+load();loadTraining();migrateProgressionTypes();loadWorkout();rptRetargetAll();ensureIdentity();try{refreshWeekPhotos();}catch(e){}if(resyncAllActivityTags())save();var _hasSetup=/[#&]setup=/.test(location.hash||"");if(_hasSetup){state.settings.onboarded=true;}else{if(state.settings.onboarded==null)state.settings.onboarded=(state.weights.length>0||num(state.settings.goalWeight)!=null||num(state.settings.startingWeight)!=null||!!(state.settings.name&&(""+state.settings.name).trim()));if(!state.settings.onboarded){state.onboarding=true;state.obStep=0;}}applyTheme(state.settings.theme||"dark");makeIcon();if(state.workout)state.view="train";render();if(syncOn()){pbRefresh(function(){m10Boot();autoSync();photoSync();});}/* M10 wiring: lease lifecycle boots with sync */if(state.workout)startWoTick();if(_hasSetup)checkSetupLink();
 endRecoveryFreeze();
 /* Everything below belongs ONLY to the clean path. A gated page is terminal:
    no persistence request, no announcement write, no update check, no visibility
