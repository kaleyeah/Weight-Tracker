diff --git a/index.html b/index.html
index 8ba7454..a6a2c9e 100644
--- a/index.html
+++ b/index.html
@@ -11119,7 +11119,31 @@ function m10pDispatch(done){
     if(en.state==="intent"){
       idbGetLocal(en.localId).then(function(existing){
         if(!authOk()){fin(false);return;}
-        if(existing){clearEntry()&&next(true);return;}   /* never overwrite local bytes */
+        if(existing){
+          /* rulings 3/4: an existing record may be THIS adoption's own blob
+             (crash after the local write, before `blob-ok`) or unrelated local
+             data. Identity-check it: our own → promote through the mapping
+             phase; unrelated → review, never silent cleanup. */
+          if(!existing.blob){setPhase({state:"unverified",unverifiedReason:"adopt-local-unreadable"});fin(false);return;}
+          m10pSha256(existing.blob).then(function(idn2){
+            if(!authOk()){fin(false);return;}
+            if(en.blobSha256&&idn2.hex!==en.blobSha256){
+              setPhase({state:"unverified",unverifiedReason:"adopt-local-differs"});fin(false);
+              try{render();}catch(ex){}
+              return;}
+            var mapped=null;try{mapped=pbPhotoMap()[en.localId]||null;}catch(ex){}
+            if(mapped&&mapped!==en.serverId){
+              setPhase({state:"unverified",unverifiedReason:"adopt-mapping-differs"});fin(false);
+              try{render();}catch(ex){}
+              return;}
+            /* our own bytes (or an unmapped local copy of the same id): finish
+               the mapping obligation rather than dropping it */
+            setPhase({state:"blob-ok",blobSha256:idn2.hex,blobByteLength:idn2.byteLength});
+            if(!mapWriteVerified(en.localId,en.serverId)){
+              m10cBlock("could not record where a photo was saved");fin(false);return;}
+            setPhase({state:"mapped"})&&clearEntry()&&next(true);
+          }).catch(function(){fin(false);});
+          return;}
         pbFileToken().then(function(tok){
           if(!authOk()){fin(false);return;}
           pbPhotoFetchBlob({id:en.serverId,file:en.file},tok).then(function(b){
@@ -11215,8 +11239,16 @@ function m10pDispatch(done){
         return;}
       if(r.status===409&&r.body&&r.body.fenceStale===true){
         if(!m10pNat(r.body.fence)){fin(false);return;}
-        setPhase({state:"displaced",displacedReason:"fence"});fin(false);
-        try{render();}catch(ex){}
+        /* ruling 5: capture the AUTHORITATIVE server identity at displacement
+           so the later destructive Apply has a pre-image to compare against */
+        pbPhotoList().then(function(list){
+          var srec=(list||[]).find(function(x){return x.id===en.serverId;});
+          setPhase(srec?{state:"displaced",displacedReason:"fence",
+              capturedServerIdentity:{recordId:srec.id,localId:srec.localId,file:srec.file}}
+            :{state:"displaced",displacedReason:"fence"});
+          fin(false);try{render();}catch(ex){}
+        }).catch(function(){
+          setPhase({state:"displaced",displacedReason:"fence"});fin(false);try{render();}catch(ex){}});
         return;}
       fin(false);
     }).catch(function(){fin(false);});
@@ -11294,17 +11326,25 @@ function m10pReviewApply(opId){
           var p2=m10pPen();
           if(!p2||p2.fence!==entryFence){toast("The active-writer pen changed — review again");return;}
           var srec=(list||[]).find(function(x){return x.id===en.serverId;});
-          if(!srec||srec.localId!==en.localId){
+          var pre=en.capturedServerIdentity||null;
+          var same=!!srec&&srec.localId===en.localId&&
+            (!pre||!pre.file||(pre.recordId===srec.id&&pre.localId===srec.localId&&pre.file===srec.file));
+          if(!same){
             m10pMutate(ctx.uid,function(ops){
               var e3=ops.find(function(x){return x.id===opId;});
               if(e3){e3.state="unverified";e3.unverifiedReason="server-identity-changed";}});
             toast("That photo is no longer the same on the server — review again");
             try{render();}catch(e){}
             return;}
-          m10pMutate(ctx.uid,function(ops){
-            var e4=ops.find(function(x){return x.id===opId;});
-            if(e4)e4.capturedServerIdentity={recordId:srec.id,localId:srec.localId,file:srec.file};});
-          requeue();
+          /* ruling 6: ONE verified transition carries both the freshly proven
+             identity and the resolution state — never two independent writes */
+          if(!m10pMutate(ctx.uid,function(ops){
+            var e4=ops.find(function(x){return x.id===opId&&
+              (x.state==="displaced"||x.state==="unverified");});
+            if(!e4)return false;
+            e4.capturedServerIdentity={recordId:srec.id,localId:srec.localId,file:srec.file};
+            e4.state="intent";})){toast("Couldn’t record the change — try again");return;}
+          m10pDispatch(function(){try{render();}catch(e){}});
         }).catch(function(){toast("No connection — try again");});
       },{label:"Delete it",danger:true});
     }).catch(function(){});
@@ -11803,6 +11843,7 @@ idbClearAll=function(){
   window.m10pQueueMeta=function(serverId,localId,oldMeta,newMeta){
     var uid=m8Uid();
     if(!uid||!syncOn()||!serverId)return false;
+    if(window.m8StorageBlocked)return false;
     var opId=m10pOpId(),rid=m10cRequestId();
     if(!opId||!rid)return false;
     var en={id:opId,op:"meta",localId:String(localId||""),serverId:String(serverId),
@@ -11922,23 +11963,33 @@ function openLightbox(id){
     var m=b.getAttribute("data-meal");
     if((r.meal||"")===m){mealsRow.style.display="none";return;}
     if(ownershipAmbiguous()){toast("Paused — this device holds data from another account");return;}
-    r.meal=m;
-    idbAddLocal(r).then(function(){
-      dirty=true;
-      var label="";MEALS.forEach(function(x){if(x[0]===m)label=x[1];});
-      toast("Moved to "+label);
-      renderMeals();
-      setTimeout(function(){mealsRow.style.display="none";},350);
-      /* mirror to the server; if unreachable, the photo-sync up-sync pass
-         pushes the label the next time it runs */
-      var sid=pbPhotoMap()[r.id];
-      /* This route does not go through pbPhotoUpload/pbPhotoDelete, so it needs
-         its own guard: a photo record PATCH is still a write to the signed-in
-         account's server data. */
-      if(syncOn()&&sid&&!ownershipAmbiguous()){
-        fetch(pbBase()+"/api/collections/photos/records/"+sid,{method:"PATCH",
-          headers:pbHeaders(true),body:JSON.stringify({meal:m})}).catch(function(){});}
-    }).catch(function(){toast("Couldn’t move the photo");});}
+    /* M10 (round-23 ruling 2): the relabel is a CONTENT mutation. The durable
+       queue entry carrying old+new metadata is written and VERIFIED before the
+       local store changes, and the server side rides the reviewed
+       transactional route — never a raw collection PATCH. */
+    var oldMeal=r.meal||"";
+    var sid=pbPhotoMap()[r.id];
+    var applyLocal=function(){
+      r.meal=m;
+      return idbAddLocal(r).then(function(){
+        dirty=true;
+        var label="";MEALS.forEach(function(x){if(x[0]===m)label=x[1];});
+        toast("Moved to "+label);
+        renderMeals();
+        setTimeout(function(){mealsRow.style.display="none";},350);
+      });};
+    if(syncOn()&&sid&&!ownershipAmbiguous()){
+      var qd=m10pQueueMeta(sid,r.id,
+        {kind:r.kind==="progress"?"progress":"food",date:String(r.date||"").slice(0,10),
+         week:String(r.week||"").slice(0,10),pose:String(r.pose||"").slice(0,20),
+         meal:String(oldMeal).slice(0,20),ts:String(r.ts||"")},
+        {kind:r.kind==="progress"?"progress":"food",date:String(r.date||"").slice(0,10),
+         week:String(r.week||"").slice(0,10),pose:String(r.pose||"").slice(0,20),
+         meal:String(m).slice(0,20),ts:String(r.ts||"")});
+      if(!qd){toast("Couldn’t record the change — try again");return;}
+      applyLocal().catch(function(){toast("Couldn’t move the photo");});
+      return;}
+    applyLocal().catch(function(){toast("Couldn’t move the photo");});}
   else if(act==="del"){var pid=ids[idx];askConfirm("Delete this photo?",function(){idbDelete(pid).then(function(){dirty=true;try{URL.revokeObjectURL(urls[pid]);}catch(e){}delete urls[pid];ids.splice(idx,1);if(!ids.length){close();return;}if(idx>=ids.length)idx=ids.length-1;show();});});}});
   var sx=0,sy=0;
   ov.addEventListener("touchstart",function(e){if(!e.touches.length)return;sx=e.touches[0].clientX;sy=e.touches[0].clientY;},{passive:true});
