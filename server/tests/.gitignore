# Disposable fixture / probe credentials — generated per run, never committed.
.fixture-creds.env
.probe-creds.env
# V15 integrity baselines: no payloads, but they carry record and user IDs.
*.sentinel.json
# Per-run suite output; the curated copies live in the review package instead.
evidence/
__pycache__/
*.pyc
